apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mina-sshd.fullname" . }}-test-connection"
  labels:
    {{- include "mina-sshd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mina-sshd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
