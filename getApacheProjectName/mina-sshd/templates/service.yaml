apiVersion: v1
kind: Service
metadata:
  name: {{ include "mina-sshd.fullname" . }}
  labels:
    {{- include "mina-sshd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mina-sshd.selectorLabels" . | nindent 4 }}
