apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-torque.fullname" . }}-test-connection"
  labels:
    {{- include "db-torque.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-torque.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
