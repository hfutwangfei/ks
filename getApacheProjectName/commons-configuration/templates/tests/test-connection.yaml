apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-configuration.fullname" . }}-test-connection"
  labels:
    {{- include "commons-configuration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-configuration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
