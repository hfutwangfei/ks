apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-hama.fullname" . }}
  labels:
    {{- include "attic-hama.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-hama.selectorLabels" . | nindent 4 }}
