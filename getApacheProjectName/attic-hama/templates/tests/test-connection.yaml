apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-hama.fullname" . }}-test-connection"
  labels:
    {{- include "attic-hama.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-hama.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
