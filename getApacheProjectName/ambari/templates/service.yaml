apiVersion: v1
kind: Service
metadata:
  name: {{ include "ambari.fullname" . }}
  labels:
    {{- include "ambari.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ambari.selectorLabels" . | nindent 4 }}
