apiVersion: v1
kind: Service
metadata:
  name: {{ include "celix.fullname" . }}
  labels:
    {{- include "celix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "celix.selectorLabels" . | nindent 4 }}
