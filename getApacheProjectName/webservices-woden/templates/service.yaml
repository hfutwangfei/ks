apiVersion: v1
kind: Service
metadata:
  name: {{ include "webservices-woden.fullname" . }}
  labels:
    {{- include "webservices-woden.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webservices-woden.selectorLabels" . | nindent 4 }}
