apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-continuum.fullname" . }}-test-connection"
  labels:
    {{- include "attic-continuum.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-continuum.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
