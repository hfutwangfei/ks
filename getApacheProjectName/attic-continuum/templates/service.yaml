apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-continuum.fullname" . }}
  labels:
    {{- include "attic-continuum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-continuum.selectorLabels" . | nindent 4 }}
