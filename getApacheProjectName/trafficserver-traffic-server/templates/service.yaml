apiVersion: v1
kind: Service
metadata:
  name: {{ include "trafficserver-traffic-server.fullname" . }}
  labels:
    {{- include "trafficserver-traffic-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trafficserver-traffic-server.selectorLabels" . | nindent 4 }}
