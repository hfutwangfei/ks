apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trafficserver-traffic-server.fullname" . }}-test-connection"
  labels:
    {{- include "trafficserver-traffic-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trafficserver-traffic-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
