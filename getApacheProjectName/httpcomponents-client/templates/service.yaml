apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpcomponents-client.fullname" . }}
  labels:
    {{- include "httpcomponents-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpcomponents-client.selectorLabels" . | nindent 4 }}
