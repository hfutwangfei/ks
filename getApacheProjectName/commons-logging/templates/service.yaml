apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-logging.fullname" . }}
  labels:
    {{- include "commons-logging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-logging.selectorLabels" . | nindent 4 }}
