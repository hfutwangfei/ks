apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-email.fullname" . }}
  labels:
    {{- include "commons-email.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-email.selectorLabels" . | nindent 4 }}
