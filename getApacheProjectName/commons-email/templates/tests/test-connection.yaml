apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-email.fullname" . }}-test-connection"
  labels:
    {{- include "commons-email.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-email.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
