apiVersion: v1
kind: Service
metadata:
  name: {{ include "directory-studio.fullname" . }}
  labels:
    {{- include "directory-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "directory-studio.selectorLabels" . | nindent 4 }}
