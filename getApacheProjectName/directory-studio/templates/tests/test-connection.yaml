apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "directory-studio.fullname" . }}-test-connection"
  labels:
    {{- include "directory-studio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "directory-studio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
