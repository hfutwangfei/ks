apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "creadur-rat.fullname" . }}-test-connection"
  labels:
    {{- include "creadur-rat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "creadur-rat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
