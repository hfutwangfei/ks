apiVersion: v1
kind: Service
metadata:
  name: {{ include "creadur-rat.fullname" . }}
  labels:
    {{- include "creadur-rat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "creadur-rat.selectorLabels" . | nindent 4 }}
