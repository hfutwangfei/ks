apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hbase.fullname" . }}-test-connection"
  labels:
    {{- include "hbase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hbase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
