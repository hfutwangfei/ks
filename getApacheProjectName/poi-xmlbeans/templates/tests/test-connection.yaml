apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poi-xmlbeans.fullname" . }}-test-connection"
  labels:
    {{- include "poi-xmlbeans.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poi-xmlbeans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
