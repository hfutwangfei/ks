apiVersion: v1
kind: Service
metadata:
  name: {{ include "gump.fullname" . }}
  labels:
    {{- include "gump.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gump.selectorLabels" . | nindent 4 }}
