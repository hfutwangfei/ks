apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gump.fullname" . }}-test-connection"
  labels:
    {{- include "gump.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gump.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
