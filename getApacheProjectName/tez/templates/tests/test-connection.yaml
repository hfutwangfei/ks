apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tez.fullname" . }}-test-connection"
  labels:
    {{- include "tez.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tez.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
