apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mahout.fullname" . }}-test-connection"
  labels:
    {{- include "mahout.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mahout.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
