apiVersion: v1
kind: Service
metadata:
  name: {{ include "vcl.fullname" . }}
  labels:
    {{- include "vcl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vcl.selectorLabels" . | nindent 4 }}
