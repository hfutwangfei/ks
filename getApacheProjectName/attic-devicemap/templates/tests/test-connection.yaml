apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-devicemap.fullname" . }}-test-connection"
  labels:
    {{- include "attic-devicemap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-devicemap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
