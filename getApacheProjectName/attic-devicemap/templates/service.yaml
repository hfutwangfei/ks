apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-devicemap.fullname" . }}
  labels:
    {{- include "attic-devicemap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-devicemap.selectorLabels" . | nindent 4 }}
