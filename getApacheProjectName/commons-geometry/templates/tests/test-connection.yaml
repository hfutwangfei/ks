apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-geometry.fullname" . }}-test-connection"
  labels:
    {{- include "commons-geometry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-geometry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
