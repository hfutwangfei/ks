apiVersion: v1
kind: Service
metadata:
  name: {{ include "avro.fullname" . }}
  labels:
    {{- include "avro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avro.selectorLabels" . | nindent 4 }}
