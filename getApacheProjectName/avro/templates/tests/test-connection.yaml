apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avro.fullname" . }}-test-connection"
  labels:
    {{- include "avro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
