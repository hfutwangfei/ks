apiVersion: v1
kind: Service
metadata:
  name: {{ include "nutch.fullname" . }}
  labels:
    {{- include "nutch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nutch.selectorLabels" . | nindent 4 }}
