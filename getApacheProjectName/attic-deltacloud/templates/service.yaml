apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-deltacloud.fullname" . }}
  labels:
    {{- include "attic-deltacloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-deltacloud.selectorLabels" . | nindent 4 }}
