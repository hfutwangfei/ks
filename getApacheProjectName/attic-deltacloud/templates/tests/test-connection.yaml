apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-deltacloud.fullname" . }}-test-connection"
  labels:
    {{- include "attic-deltacloud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-deltacloud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
