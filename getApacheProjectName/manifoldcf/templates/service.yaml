apiVersion: v1
kind: Service
metadata:
  name: {{ include "manifoldcf.fullname" . }}
  labels:
    {{- include "manifoldcf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manifoldcf.selectorLabels" . | nindent 4 }}
