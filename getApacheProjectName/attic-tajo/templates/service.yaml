apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-tajo.fullname" . }}
  labels:
    {{- include "attic-tajo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-tajo.selectorLabels" . | nindent 4 }}
