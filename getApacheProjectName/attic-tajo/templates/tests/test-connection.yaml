apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-tajo.fullname" . }}-test-connection"
  labels:
    {{- include "attic-tajo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-tajo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
