apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xerces-xml-commons-resolver.fullname" . }}-test-connection"
  labels:
    {{- include "xerces-xml-commons-resolver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xerces-xml-commons-resolver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
