apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-antunit.fullname" . }}
  labels:
    {{- include "ant-antunit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-antunit.selectorLabels" . | nindent 4 }}
