apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant-antunit.fullname" . }}-test-connection"
  labels:
    {{- include "ant-antunit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant-antunit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
