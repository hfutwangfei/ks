apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-shale.fullname" . }}
  labels:
    {{- include "attic-shale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-shale.selectorLabels" . | nindent 4 }}
