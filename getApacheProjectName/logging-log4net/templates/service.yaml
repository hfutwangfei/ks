apiVersion: v1
kind: Service
metadata:
  name: {{ include "logging-log4net.fullname" . }}
  labels:
    {{- include "logging-log4net.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logging-log4net.selectorLabels" . | nindent 4 }}
