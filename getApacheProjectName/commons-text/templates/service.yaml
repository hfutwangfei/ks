apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-text.fullname" . }}
  labels:
    {{- include "commons-text.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-text.selectorLabels" . | nindent 4 }}
