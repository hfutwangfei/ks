apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steve.fullname" . }}-test-connection"
  labels:
    {{- include "steve.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "steve.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
