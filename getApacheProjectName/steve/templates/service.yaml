apiVersion: v1
kind: Service
metadata:
  name: {{ include "steve.fullname" . }}
  labels:
    {{- include "steve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "steve.selectorLabels" . | nindent 4 }}
