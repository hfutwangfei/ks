apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-river.fullname" . }}
  labels:
    {{- include "attic-river.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-river.selectorLabels" . | nindent 4 }}
