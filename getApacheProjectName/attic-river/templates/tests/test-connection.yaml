apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-river.fullname" . }}-test-connection"
  labels:
    {{- include "attic-river.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-river.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
