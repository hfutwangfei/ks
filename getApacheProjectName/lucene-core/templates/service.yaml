apiVersion: v1
kind: Service
metadata:
  name: {{ include "lucene-core.fullname" . }}
  labels:
    {{- include "lucene-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lucene-core.selectorLabels" . | nindent 4 }}
