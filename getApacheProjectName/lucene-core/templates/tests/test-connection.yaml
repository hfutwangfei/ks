apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lucene-core.fullname" . }}-test-connection"
  labels:
    {{- include "lucene-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lucene-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
