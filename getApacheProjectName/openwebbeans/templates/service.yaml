apiVersion: v1
kind: Service
metadata:
  name: {{ include "openwebbeans.fullname" . }}
  labels:
    {{- include "openwebbeans.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openwebbeans.selectorLabels" . | nindent 4 }}
