apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openwebbeans.fullname" . }}-test-connection"
  labels:
    {{- include "openwebbeans.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openwebbeans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
