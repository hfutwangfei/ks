apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-functor.fullname" . }}
  labels:
    {{- include "commons-functor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-functor.selectorLabels" . | nindent 4 }}
