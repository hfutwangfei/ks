apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-functor.fullname" . }}-test-connection"
  labels:
    {{- include "commons-functor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-functor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
