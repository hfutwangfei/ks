apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-codec.fullname" . }}
  labels:
    {{- include "commons-codec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-codec.selectorLabels" . | nindent 4 }}
