apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpd-http-server.fullname" . }}
  labels:
    {{- include "httpd-http-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpd-http-server.selectorLabels" . | nindent 4 }}
