apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httpd-http-server.fullname" . }}-test-connection"
  labels:
    {{- include "httpd-http-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httpd-http-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
