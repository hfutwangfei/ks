apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-lens.fullname" . }}-test-connection"
  labels:
    {{- include "attic-lens.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-lens.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
