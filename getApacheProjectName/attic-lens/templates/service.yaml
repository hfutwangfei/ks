apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-lens.fullname" . }}
  labels:
    {{- include "attic-lens.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-lens.selectorLabels" . | nindent 4 }}
