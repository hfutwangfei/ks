apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sis-spatial-information-system.fullname" . }}-test-connection"
  labels:
    {{- include "sis-spatial-information-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sis-spatial-information-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
