apiVersion: v1
kind: Service
metadata:
  name: {{ include "velocity-dvsl.fullname" . }}
  labels:
    {{- include "velocity-dvsl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "velocity-dvsl.selectorLabels" . | nindent 4 }}
