apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velocity-dvsl.fullname" . }}-test-connection"
  labels:
    {{- include "velocity-dvsl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "velocity-dvsl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
