apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-buildr.fullname" . }}
  labels:
    {{- include "attic-buildr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-buildr.selectorLabels" . | nindent 4 }}
