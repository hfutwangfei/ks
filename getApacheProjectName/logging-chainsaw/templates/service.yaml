apiVersion: v1
kind: Service
metadata:
  name: {{ include "logging-chainsaw.fullname" . }}
  labels:
    {{- include "logging-chainsaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logging-chainsaw.selectorLabels" . | nindent 4 }}
