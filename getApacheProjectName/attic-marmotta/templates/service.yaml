apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-marmotta.fullname" . }}
  labels:
    {{- include "attic-marmotta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-marmotta.selectorLabels" . | nindent 4 }}
