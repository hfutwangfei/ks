apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-marmotta.fullname" . }}-test-connection"
  labels:
    {{- include "attic-marmotta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-marmotta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
