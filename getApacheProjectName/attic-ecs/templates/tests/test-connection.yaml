apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-ecs.fullname" . }}-test-connection"
  labels:
    {{- include "attic-ecs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-ecs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
