apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-ecs.fullname" . }}
  labels:
    {{- include "attic-ecs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-ecs.selectorLabels" . | nindent 4 }}
