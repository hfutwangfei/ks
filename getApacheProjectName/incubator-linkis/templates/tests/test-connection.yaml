apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incubator-linkis.fullname" . }}-test-connection"
  labels:
    {{- include "incubator-linkis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incubator-linkis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
