apiVersion: v1
kind: Service
metadata:
  name: {{ include "incubator-linkis.fullname" . }}
  labels:
    {{- include "incubator-linkis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incubator-linkis.selectorLabels" . | nindent 4 }}
