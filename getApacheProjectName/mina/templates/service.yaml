apiVersion: v1
kind: Service
metadata:
  name: {{ include "mina.fullname" . }}
  labels:
    {{- include "mina.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mina.selectorLabels" . | nindent 4 }}
