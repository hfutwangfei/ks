apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mina.fullname" . }}-test-connection"
  labels:
    {{- include "mina.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mina.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
