apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-ace.fullname" . }}-test-connection"
  labels:
    {{- include "attic-ace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-ace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
