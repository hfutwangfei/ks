apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-wink.fullname" . }}-test-connection"
  labels:
    {{- include "attic-wink.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-wink.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
