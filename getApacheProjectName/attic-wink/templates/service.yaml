apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-wink.fullname" . }}
  labels:
    {{- include "attic-wink.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-wink.selectorLabels" . | nindent 4 }}
