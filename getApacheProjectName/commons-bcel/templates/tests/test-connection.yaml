apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-bcel.fullname" . }}-test-connection"
  labels:
    {{- include "commons-bcel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-bcel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
