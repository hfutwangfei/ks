apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-bcel.fullname" . }}
  labels:
    {{- include "commons-bcel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-bcel.selectorLabels" . | nindent 4 }}
