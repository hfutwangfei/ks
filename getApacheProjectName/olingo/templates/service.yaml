apiVersion: v1
kind: Service
metadata:
  name: {{ include "olingo.fullname" . }}
  labels:
    {{- include "olingo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "olingo.selectorLabels" . | nindent 4 }}
