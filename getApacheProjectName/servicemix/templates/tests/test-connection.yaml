apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servicemix.fullname" . }}-test-connection"
  labels:
    {{- include "servicemix.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "servicemix.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
