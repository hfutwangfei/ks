apiVersion: v1
kind: Service
metadata:
  name: {{ include "aries.fullname" . }}
  labels:
    {{- include "aries.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aries.selectorLabels" . | nindent 4 }}
