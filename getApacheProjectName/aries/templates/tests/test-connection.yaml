apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aries.fullname" . }}-test-connection"
  labels:
    {{- include "aries.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aries.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
