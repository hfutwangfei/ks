apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-beehive.fullname" . }}-test-connection"
  labels:
    {{- include "attic-beehive.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-beehive.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
