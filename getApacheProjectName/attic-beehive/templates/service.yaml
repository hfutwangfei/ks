apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-beehive.fullname" . }}
  labels:
    {{- include "attic-beehive.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-beehive.selectorLabels" . | nindent 4 }}
