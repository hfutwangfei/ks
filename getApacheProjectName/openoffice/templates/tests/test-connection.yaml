apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openoffice.fullname" . }}-test-connection"
  labels:
    {{- include "openoffice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openoffice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
