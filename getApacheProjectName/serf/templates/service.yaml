apiVersion: v1
kind: Service
metadata:
  name: {{ include "serf.fullname" . }}
  labels:
    {{- include "serf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "serf.selectorLabels" . | nindent 4 }}
