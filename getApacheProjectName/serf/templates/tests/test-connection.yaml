apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "serf.fullname" . }}-test-connection"
  labels:
    {{- include "serf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "serf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
