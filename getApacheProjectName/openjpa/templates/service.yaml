apiVersion: v1
kind: Service
metadata:
  name: {{ include "openjpa.fullname" . }}
  labels:
    {{- include "openjpa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openjpa.selectorLabels" . | nindent 4 }}
