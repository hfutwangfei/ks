apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openjpa.fullname" . }}-test-connection"
  labels:
    {{- include "openjpa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openjpa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
