apiVersion: v1
kind: Service
metadata:
  name: {{ include "comdev-community-development.fullname" . }}
  labels:
    {{- include "comdev-community-development.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "comdev-community-development.selectorLabels" . | nindent 4 }}
