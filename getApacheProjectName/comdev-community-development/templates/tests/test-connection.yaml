apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "comdev-community-development.fullname" . }}-test-connection"
  labels:
    {{- include "comdev-community-development.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "comdev-community-development.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
