apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant-props.fullname" . }}-test-connection"
  labels:
    {{- include "ant-props.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant-props.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
