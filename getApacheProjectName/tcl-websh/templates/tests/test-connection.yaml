apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tcl-websh.fullname" . }}-test-connection"
  labels:
    {{- include "tcl-websh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tcl-websh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
