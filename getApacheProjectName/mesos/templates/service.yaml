apiVersion: v1
kind: Service
metadata:
  name: {{ include "mesos.fullname" . }}
  labels:
    {{- include "mesos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mesos.selectorLabels" . | nindent 4 }}
