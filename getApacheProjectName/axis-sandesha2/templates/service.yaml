apiVersion: v1
kind: Service
metadata:
  name: {{ include "axis-sandesha2.fullname" . }}
  labels:
    {{- include "axis-sandesha2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "axis-sandesha2.selectorLabels" . | nindent 4 }}
