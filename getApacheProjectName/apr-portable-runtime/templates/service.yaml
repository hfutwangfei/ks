apiVersion: v1
kind: Service
metadata:
  name: {{ include "apr-portable-runtime.fullname" . }}
  labels:
    {{- include "apr-portable-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apr-portable-runtime.selectorLabels" . | nindent 4 }}
