apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apr-portable-runtime.fullname" . }}-test-connection"
  labels:
    {{- include "apr-portable-runtime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apr-portable-runtime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
