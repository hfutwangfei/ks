apiVersion: v1
kind: Service
metadata:
  name: {{ include "myfaces.fullname" . }}
  labels:
    {{- include "myfaces.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myfaces.selectorLabels" . | nindent 4 }}
