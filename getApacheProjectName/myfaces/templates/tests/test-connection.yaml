apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfaces.fullname" . }}-test-connection"
  labels:
    {{- include "myfaces.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myfaces.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
