apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-vxquery.fullname" . }}-test-connection"
  labels:
    {{- include "attic-vxquery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-vxquery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
