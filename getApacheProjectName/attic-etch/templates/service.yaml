apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-etch.fullname" . }}
  labels:
    {{- include "attic-etch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-etch.selectorLabels" . | nindent 4 }}
