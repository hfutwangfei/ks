apiVersion: v1
kind: Service
metadata:
  name: {{ include "creadur-tentacles.fullname" . }}
  labels:
    {{- include "creadur-tentacles.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "creadur-tentacles.selectorLabels" . | nindent 4 }}
