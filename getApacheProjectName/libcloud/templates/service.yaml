apiVersion: v1
kind: Service
metadata:
  name: {{ include "libcloud.fullname" . }}
  labels:
    {{- include "libcloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "libcloud.selectorLabels" . | nindent 4 }}
