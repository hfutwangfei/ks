apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "libcloud.fullname" . }}-test-connection"
  labels:
    {{- include "libcloud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "libcloud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
