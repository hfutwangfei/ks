apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hudi.fullname" . }}-test-connection"
  labels:
    {{- include "hudi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hudi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
