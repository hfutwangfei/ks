apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kudu.fullname" . }}-test-connection"
  labels:
    {{- include "kudu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kudu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
