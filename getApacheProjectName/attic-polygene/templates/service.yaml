apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-polygene.fullname" . }}
  labels:
    {{- include "attic-polygene.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-polygene.selectorLabels" . | nindent 4 }}
