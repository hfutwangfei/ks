apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-polygene.fullname" . }}-test-connection"
  labels:
    {{- include "attic-polygene.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-polygene.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
