apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-io.fullname" . }}-test-connection"
  labels:
    {{- include "commons-io.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
