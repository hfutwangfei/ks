apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-io.fullname" . }}
  labels:
    {{- include "commons-io.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-io.selectorLabels" . | nindent 4 }}
