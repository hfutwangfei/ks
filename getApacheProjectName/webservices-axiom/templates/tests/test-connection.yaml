apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webservices-axiom.fullname" . }}-test-connection"
  labels:
    {{- include "webservices-axiom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webservices-axiom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
