apiVersion: v1
kind: Service
metadata:
  name: {{ include "webservices-axiom.fullname" . }}
  labels:
    {{- include "webservices-axiom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webservices-axiom.selectorLabels" . | nindent 4 }}
