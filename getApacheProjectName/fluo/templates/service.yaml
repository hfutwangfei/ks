apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluo.fullname" . }}
  labels:
    {{- include "fluo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluo.selectorLabels" . | nindent 4 }}
