apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluo.fullname" . }}-test-connection"
  labels:
    {{- include "fluo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
