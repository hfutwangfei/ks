apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cayenne.fullname" . }}-test-connection"
  labels:
    {{- include "cayenne.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cayenne.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
