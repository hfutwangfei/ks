apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-directmemory.fullname" . }}
  labels:
    {{- include "attic-directmemory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-directmemory.selectorLabels" . | nindent 4 }}
