apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jackrabbit.fullname" . }}-test-connection"
  labels:
    {{- include "jackrabbit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jackrabbit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
