apiVersion: v1
kind: Service
metadata:
  name: {{ include "jackrabbit.fullname" . }}
  labels:
    {{- include "jackrabbit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jackrabbit.selectorLabels" . | nindent 4 }}
