apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-chemistry.fullname" . }}-test-connection"
  labels:
    {{- include "attic-chemistry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-chemistry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
