apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-chemistry.fullname" . }}
  labels:
    {{- include "attic-chemistry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-chemistry.selectorLabels" . | nindent 4 }}
