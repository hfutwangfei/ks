apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-numbers.fullname" . }}-test-connection"
  labels:
    {{- include "commons-numbers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-numbers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
