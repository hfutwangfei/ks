apiVersion: v1
kind: Service
metadata:
  name: {{ include "sling.fullname" . }}
  labels:
    {{- include "sling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sling.selectorLabels" . | nindent 4 }}
