apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sling.fullname" . }}-test-connection"
  labels:
    {{- include "sling.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
