apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "axis-axis2.fullname" . }}-test-connection"
  labels:
    {{- include "axis-axis2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "axis-axis2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
