apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant-ivyde.fullname" . }}-test-connection"
  labels:
    {{- include "ant-ivyde.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant-ivyde.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
