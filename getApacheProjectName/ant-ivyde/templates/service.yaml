apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-ivyde.fullname" . }}
  labels:
    {{- include "ant-ivyde.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-ivyde.selectorLabels" . | nindent 4 }}
