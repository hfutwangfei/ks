apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-weaver.fullname" . }}
  labels:
    {{- include "commons-weaver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-weaver.selectorLabels" . | nindent 4 }}
