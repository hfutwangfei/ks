apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-weaver.fullname" . }}-test-connection"
  labels:
    {{- include "commons-weaver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-weaver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
