apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-oltu---parent.fullname" . }}-test-connection"
  labels:
    {{- include "attic-oltu---parent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-oltu---parent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
