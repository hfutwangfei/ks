apiVersion: v1
kind: Service
metadata:
  name: {{ include "samza.fullname" . }}
  labels:
    {{- include "samza.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "samza.selectorLabels" . | nindent 4 }}
