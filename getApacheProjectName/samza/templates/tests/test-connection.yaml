apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "samza.fullname" . }}-test-connection"
  labels:
    {{- include "samza.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "samza.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
