apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-jcs.fullname" . }}
  labels:
    {{- include "commons-jcs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-jcs.selectorLabels" . | nindent 4 }}
