apiVersion: v1
kind: Service
metadata:
  name: {{ include "mina-ftpserver.fullname" . }}
  labels:
    {{- include "mina-ftpserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mina-ftpserver.selectorLabels" . | nindent 4 }}
