apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-harmony.fullname" . }}
  labels:
    {{- include "attic-harmony.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-harmony.selectorLabels" . | nindent 4 }}
