apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "felix.fullname" . }}-test-connection"
  labels:
    {{- include "felix.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "felix.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
