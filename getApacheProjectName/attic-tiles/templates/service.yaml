apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-tiles.fullname" . }}
  labels:
    {{- include "attic-tiles.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-tiles.selectorLabels" . | nindent 4 }}
