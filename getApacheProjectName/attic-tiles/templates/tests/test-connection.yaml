apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-tiles.fullname" . }}-test-connection"
  labels:
    {{- include "attic-tiles.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-tiles.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
