apiVersion: v1
kind: Service
metadata:
  name: {{ include "incubator-annotator.fullname" . }}
  labels:
    {{- include "incubator-annotator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incubator-annotator.selectorLabels" . | nindent 4 }}
