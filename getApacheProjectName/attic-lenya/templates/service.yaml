apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-lenya.fullname" . }}
  labels:
    {{- include "attic-lenya.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-lenya.selectorLabels" . | nindent 4 }}
