apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-dbutils.fullname" . }}
  labels:
    {{- include "commons-dbutils.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-dbutils.selectorLabels" . | nindent 4 }}
