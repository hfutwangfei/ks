apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-dbutils.fullname" . }}-test-connection"
  labels:
    {{- include "commons-dbutils.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-dbutils.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
