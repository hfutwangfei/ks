apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-cli.fullname" . }}
  labels:
    {{- include "commons-cli.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-cli.selectorLabels" . | nindent 4 }}
