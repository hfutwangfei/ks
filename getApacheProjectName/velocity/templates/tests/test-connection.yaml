apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velocity.fullname" . }}-test-connection"
  labels:
    {{- include "velocity.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "velocity.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
