apiVersion: v1
kind: Service
metadata:
  name: {{ include "xalan-for-cpp-xslt-processor.fullname" . }}
  labels:
    {{- include "xalan-for-cpp-xslt-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xalan-for-cpp-xslt-processor.selectorLabels" . | nindent 4 }}
