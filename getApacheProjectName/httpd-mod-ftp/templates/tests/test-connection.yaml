apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httpd-mod-ftp.fullname" . }}-test-connection"
  labels:
    {{- include "httpd-mod-ftp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httpd-mod-ftp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
