apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-jexl.fullname" . }}
  labels:
    {{- include "commons-jexl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-jexl.selectorLabels" . | nindent 4 }}
