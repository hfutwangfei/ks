apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cordova.fullname" . }}-test-connection"
  labels:
    {{- include "cordova.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cordova.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
