apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-open-climate-workbench.fullname" . }}
  labels:
    {{- include "attic-open-climate-workbench.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-open-climate-workbench.selectorLabels" . | nindent 4 }}
