apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-open-climate-workbench.fullname" . }}-test-connection"
  labels:
    {{- include "attic-open-climate-workbench.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-open-climate-workbench.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
