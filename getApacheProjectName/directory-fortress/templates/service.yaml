apiVersion: v1
kind: Service
metadata:
  name: {{ include "directory-fortress.fullname" . }}
  labels:
    {{- include "directory-fortress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "directory-fortress.selectorLabels" . | nindent 4 }}
