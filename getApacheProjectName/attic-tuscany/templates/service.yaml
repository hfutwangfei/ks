apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-tuscany.fullname" . }}
  labels:
    {{- include "attic-tuscany.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-tuscany.selectorLabels" . | nindent 4 }}
