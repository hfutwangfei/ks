apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-chukwa.fullname" . }}-test-connection"
  labels:
    {{- include "attic-chukwa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-chukwa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
