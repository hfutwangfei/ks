apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incubator-milagro.fullname" . }}-test-connection"
  labels:
    {{- include "incubator-milagro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incubator-milagro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
