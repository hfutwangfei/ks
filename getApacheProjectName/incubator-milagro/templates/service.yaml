apiVersion: v1
kind: Service
metadata:
  name: {{ include "incubator-milagro.fullname" . }}
  labels:
    {{- include "incubator-milagro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incubator-milagro.selectorLabels" . | nindent 4 }}
