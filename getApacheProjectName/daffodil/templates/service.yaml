apiVersion: v1
kind: Service
metadata:
  name: {{ include "daffodil.fullname" . }}
  labels:
    {{- include "daffodil.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daffodil.selectorLabels" . | nindent 4 }}
