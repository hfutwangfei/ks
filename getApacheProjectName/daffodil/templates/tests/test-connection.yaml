apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "daffodil.fullname" . }}-test-connection"
  labels:
    {{- include "daffodil.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "daffodil.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
