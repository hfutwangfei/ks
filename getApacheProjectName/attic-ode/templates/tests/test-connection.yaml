apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-ode.fullname" . }}-test-connection"
  labels:
    {{- include "attic-ode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-ode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
