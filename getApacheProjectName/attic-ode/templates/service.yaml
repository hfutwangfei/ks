apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-ode.fullname" . }}
  labels:
    {{- include "attic-ode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-ode.selectorLabels" . | nindent 4 }}
