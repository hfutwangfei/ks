apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-excalibur.fullname" . }}
  labels:
    {{- include "attic-excalibur.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-excalibur.selectorLabels" . | nindent 4 }}
