apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-excalibur.fullname" . }}-test-connection"
  labels:
    {{- include "attic-excalibur.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-excalibur.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
