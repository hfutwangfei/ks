apiVersion: v1
kind: Service
metadata:
  name: {{ include "geode.fullname" . }}
  labels:
    {{- include "geode.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geode.selectorLabels" . | nindent 4 }}
