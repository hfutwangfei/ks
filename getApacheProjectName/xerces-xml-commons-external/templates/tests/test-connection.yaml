apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xerces-xml-commons-external.fullname" . }}-test-connection"
  labels:
    {{- include "xerces-xml-commons-external.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xerces-xml-commons-external.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
