apiVersion: v1
kind: Service
metadata:
  name: {{ include "xerces-xml-commons-external.fullname" . }}
  labels:
    {{- include "xerces-xml-commons-external.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xerces-xml-commons-external.selectorLabels" . | nindent 4 }}
