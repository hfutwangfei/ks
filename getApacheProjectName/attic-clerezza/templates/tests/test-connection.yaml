apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-clerezza.fullname" . }}-test-connection"
  labels:
    {{- include "attic-clerezza.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-clerezza.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
