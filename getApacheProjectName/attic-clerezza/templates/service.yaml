apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-clerezza.fullname" . }}
  labels:
    {{- include "attic-clerezza.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-clerezza.selectorLabels" . | nindent 4 }}
