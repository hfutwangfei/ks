apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "subversion.fullname" . }}-test-connection"
  labels:
    {{- include "subversion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "subversion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
