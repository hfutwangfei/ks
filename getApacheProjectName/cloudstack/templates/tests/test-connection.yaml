apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudstack.fullname" . }}-test-connection"
  labels:
    {{- include "cloudstack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudstack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
