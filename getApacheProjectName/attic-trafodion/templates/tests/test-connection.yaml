apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-trafodion.fullname" . }}-test-connection"
  labels:
    {{- include "attic-trafodion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-trafodion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
