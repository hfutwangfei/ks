apiVersion: v1
kind: Service
metadata:
  name: {{ include "myfaces-tobago.fullname" . }}
  labels:
    {{- include "myfaces-tobago.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myfaces-tobago.selectorLabels" . | nindent 4 }}
