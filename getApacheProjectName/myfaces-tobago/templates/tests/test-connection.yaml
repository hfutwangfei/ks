apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfaces-tobago.fullname" . }}-test-connection"
  labels:
    {{- include "myfaces-tobago.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myfaces-tobago.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
