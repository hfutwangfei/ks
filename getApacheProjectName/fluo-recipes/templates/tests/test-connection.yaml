apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluo-recipes.fullname" . }}-test-connection"
  labels:
    {{- include "fluo-recipes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluo-recipes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
