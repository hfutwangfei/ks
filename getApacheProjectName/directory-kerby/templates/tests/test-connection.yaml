apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "directory-kerby.fullname" . }}-test-connection"
  labels:
    {{- include "directory-kerby.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "directory-kerby.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
