apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-dbcp.fullname" . }}-test-connection"
  labels:
    {{- include "commons-dbcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-dbcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
