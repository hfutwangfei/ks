apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tomee.fullname" . }}-test-connection"
  labels:
    {{- include "tomee.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tomee.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
