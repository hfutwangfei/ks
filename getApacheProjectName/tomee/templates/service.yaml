apiVersion: v1
kind: Service
metadata:
  name: {{ include "tomee.fullname" . }}
  labels:
    {{- include "tomee.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tomee.selectorLabels" . | nindent 4 }}
