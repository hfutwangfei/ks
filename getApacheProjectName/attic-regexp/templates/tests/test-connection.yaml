apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-regexp.fullname" . }}-test-connection"
  labels:
    {{- include "attic-regexp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-regexp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
