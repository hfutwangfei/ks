apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-pool.fullname" . }}
  labels:
    {{- include "commons-pool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-pool.selectorLabels" . | nindent 4 }}
