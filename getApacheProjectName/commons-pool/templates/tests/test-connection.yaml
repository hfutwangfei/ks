apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-pool.fullname" . }}-test-connection"
  labels:
    {{- include "commons-pool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-pool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
