apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-hivemind.fullname" . }}
  labels:
    {{- include "attic-hivemind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-hivemind.selectorLabels" . | nindent 4 }}
