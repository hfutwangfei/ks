apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-hivemind.fullname" . }}-test-connection"
  labels:
    {{- include "attic-hivemind.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-hivemind.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
