apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cxf.fullname" . }}-test-connection"
  labels:
    {{- include "cxf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cxf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
