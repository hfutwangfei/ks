apiVersion: v1
kind: Service
metadata:
  name: {{ include "cxf.fullname" . }}
  labels:
    {{- include "cxf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cxf.selectorLabels" . | nindent 4 }}
