{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "xmlgraphics-xml-graphics-commons.serviceAccountName" . }}
  labels:
    {{- include "xmlgraphics-xml-graphics-commons.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
