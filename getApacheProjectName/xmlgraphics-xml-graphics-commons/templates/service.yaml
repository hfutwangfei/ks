apiVersion: v1
kind: Service
metadata:
  name: {{ include "xmlgraphics-xml-graphics-commons.fullname" . }}
  labels:
    {{- include "xmlgraphics-xml-graphics-commons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xmlgraphics-xml-graphics-commons.selectorLabels" . | nindent 4 }}
