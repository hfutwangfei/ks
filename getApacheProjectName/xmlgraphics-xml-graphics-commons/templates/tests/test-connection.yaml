apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xmlgraphics-xml-graphics-commons.fullname" . }}-test-connection"
  labels:
    {{- include "xmlgraphics-xml-graphics-commons.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xmlgraphics-xml-graphics-commons.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
