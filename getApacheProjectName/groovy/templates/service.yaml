apiVersion: v1
kind: Service
metadata:
  name: {{ include "groovy.fullname" . }}
  labels:
    {{- include "groovy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "groovy.selectorLabels" . | nindent 4 }}
