apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-bsf.fullname" . }}
  labels:
    {{- include "commons-bsf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-bsf.selectorLabels" . | nindent 4 }}
