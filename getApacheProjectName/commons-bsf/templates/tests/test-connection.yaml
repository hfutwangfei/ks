apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-bsf.fullname" . }}-test-connection"
  labels:
    {{- include "commons-bsf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-bsf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
