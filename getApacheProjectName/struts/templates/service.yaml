apiVersion: v1
kind: Service
metadata:
  name: {{ include "struts.fullname" . }}
  labels:
    {{- include "struts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "struts.selectorLabels" . | nindent 4 }}
