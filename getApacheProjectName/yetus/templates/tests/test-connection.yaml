apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yetus.fullname" . }}-test-connection"
  labels:
    {{- include "yetus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yetus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
