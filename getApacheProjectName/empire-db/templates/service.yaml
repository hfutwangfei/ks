apiVersion: v1
kind: Service
metadata:
  name: {{ include "empire-db.fullname" . }}
  labels:
    {{- include "empire-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "empire-db.selectorLabels" . | nindent 4 }}
