apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opennlp.fullname" . }}-test-connection"
  labels:
    {{- include "opennlp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opennlp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
