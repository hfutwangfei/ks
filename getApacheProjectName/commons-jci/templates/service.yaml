apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-jci.fullname" . }}
  labels:
    {{- include "commons-jci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-jci.selectorLabels" . | nindent 4 }}
