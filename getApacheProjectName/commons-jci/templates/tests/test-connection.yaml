apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-jci.fullname" . }}-test-connection"
  labels:
    {{- include "commons-jci.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-jci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
