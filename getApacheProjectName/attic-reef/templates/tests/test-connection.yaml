apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-reef.fullname" . }}-test-connection"
  labels:
    {{- include "attic-reef.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-reef.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
