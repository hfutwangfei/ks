apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-reef.fullname" . }}
  labels:
    {{- include "attic-reef.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-reef.selectorLabels" . | nindent 4 }}
