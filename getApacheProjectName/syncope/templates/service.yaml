apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncope.fullname" . }}
  labels:
    {{- include "syncope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syncope.selectorLabels" . | nindent 4 }}
