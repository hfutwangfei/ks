apiVersion: v1
kind: Service
metadata:
  name: {{ include "allura.fullname" . }}
  labels:
    {{- include "allura.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "allura.selectorLabels" . | nindent 4 }}
