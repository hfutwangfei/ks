apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "allura.fullname" . }}-test-connection"
  labels:
    {{- include "allura.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "allura.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
