apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-falcon.fullname" . }}-test-connection"
  labels:
    {{- include "attic-falcon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-falcon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
