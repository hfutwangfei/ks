apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-falcon.fullname" . }}
  labels:
    {{- include "attic-falcon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-falcon.selectorLabels" . | nindent 4 }}
