apiVersion: v1
kind: Service
metadata:
  name: {{ include "logging-log4cxx.fullname" . }}
  labels:
    {{- include "logging-log4cxx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logging-log4cxx.selectorLabels" . | nindent 4 }}
