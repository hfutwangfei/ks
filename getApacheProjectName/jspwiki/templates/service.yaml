apiVersion: v1
kind: Service
metadata:
  name: {{ include "jspwiki.fullname" . }}
  labels:
    {{- include "jspwiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jspwiki.selectorLabels" . | nindent 4 }}
