apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-vss.fullname" . }}
  labels:
    {{- include "ant-vss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-vss.selectorLabels" . | nindent 4 }}
