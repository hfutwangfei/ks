apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant-vss.fullname" . }}-test-connection"
  labels:
    {{- include "ant-vss.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant-vss.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
