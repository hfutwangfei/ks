apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trafficcontrol-traffic-control.fullname" . }}-test-connection"
  labels:
    {{- include "trafficcontrol-traffic-control.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trafficcontrol-traffic-control.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
