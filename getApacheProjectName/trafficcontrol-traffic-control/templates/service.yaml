apiVersion: v1
kind: Service
metadata:
  name: {{ include "trafficcontrol-traffic-control.fullname" . }}
  labels:
    {{- include "trafficcontrol-traffic-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trafficcontrol-traffic-control.selectorLabels" . | nindent 4 }}
