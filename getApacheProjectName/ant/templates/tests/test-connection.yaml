apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant.fullname" . }}-test-connection"
  labels:
    {{- include "ant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
