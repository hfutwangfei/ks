apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-chain.fullname" . }}-test-connection"
  labels:
    {{- include "commons-chain.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-chain.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
