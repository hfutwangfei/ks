apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpcomponents-commons-httpclient.fullname" . }}
  labels:
    {{- include "httpcomponents-commons-httpclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpcomponents-commons-httpclient.selectorLabels" . | nindent 4 }}
