apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httpcomponents-commons-httpclient.fullname" . }}-test-connection"
  labels:
    {{- include "httpcomponents-commons-httpclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httpcomponents-commons-httpclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
