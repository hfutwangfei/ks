apiVersion: v1
kind: Service
metadata:
  name: {{ include "xmlgraphics-fop.fullname" . }}
  labels:
    {{- include "xmlgraphics-fop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xmlgraphics-fop.selectorLabels" . | nindent 4 }}
