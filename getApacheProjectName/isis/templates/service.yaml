apiVersion: v1
kind: Service
metadata:
  name: {{ include "isis.fullname" . }}
  labels:
    {{- include "isis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isis.selectorLabels" . | nindent 4 }}
