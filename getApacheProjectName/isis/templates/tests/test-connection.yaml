apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isis.fullname" . }}-test-connection"
  labels:
    {{- include "isis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
