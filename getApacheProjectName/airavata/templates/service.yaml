apiVersion: v1
kind: Service
metadata:
  name: {{ include "airavata.fullname" . }}
  labels:
    {{- include "airavata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airavata.selectorLabels" . | nindent 4 }}
