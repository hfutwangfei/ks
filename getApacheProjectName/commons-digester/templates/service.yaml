apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-digester.fullname" . }}
  labels:
    {{- include "commons-digester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-digester.selectorLabels" . | nindent 4 }}
