apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-digester.fullname" . }}-test-connection"
  labels:
    {{- include "commons-digester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-digester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
