apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incubator-mxnet.fullname" . }}-test-connection"
  labels:
    {{- include "incubator-mxnet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incubator-mxnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
