apiVersion: v1
kind: Service
metadata:
  name: {{ include "incubator-mxnet.fullname" . }}
  labels:
    {{- include "incubator-mxnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incubator-mxnet.selectorLabels" . | nindent 4 }}
