apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-net.fullname" . }}-test-connection"
  labels:
    {{- include "commons-net.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-net.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
