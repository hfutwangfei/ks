apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incubator-freemarker.fullname" . }}-test-connection"
  labels:
    {{- include "incubator-freemarker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incubator-freemarker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
