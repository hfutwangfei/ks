apiVersion: v1
kind: Service
metadata:
  name: {{ include "incubator-freemarker.fullname" . }}
  labels:
    {{- include "incubator-freemarker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "incubator-freemarker.selectorLabels" . | nindent 4 }}
