apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netbeans.fullname" . }}-test-connection"
  labels:
    {{- include "netbeans.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netbeans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
