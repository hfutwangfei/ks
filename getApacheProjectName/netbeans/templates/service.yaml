apiVersion: v1
kind: Service
metadata:
  name: {{ include "netbeans.fullname" . }}
  labels:
    {{- include "netbeans.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netbeans.selectorLabels" . | nindent 4 }}
