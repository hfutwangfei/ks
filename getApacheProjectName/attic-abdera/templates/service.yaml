apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-abdera.fullname" . }}
  labels:
    {{- include "attic-abdera.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-abdera.selectorLabels" . | nindent 4 }}
