apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-abdera.fullname" . }}-test-connection"
  labels:
    {{- include "attic-abdera.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-abdera.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
