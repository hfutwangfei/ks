apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iotdb.fullname" . }}-test-connection"
  labels:
    {{- include "iotdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iotdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
