apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-daemon.fullname" . }}
  labels:
    {{- include "commons-daemon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-daemon.selectorLabels" . | nindent 4 }}
