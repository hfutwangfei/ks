apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poi.fullname" . }}-test-connection"
  labels:
    {{- include "poi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
