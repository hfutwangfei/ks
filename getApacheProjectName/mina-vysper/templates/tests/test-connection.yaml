apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mina-vysper.fullname" . }}-test-connection"
  labels:
    {{- include "mina-vysper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mina-vysper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
