apiVersion: v1
kind: Service
metadata:
  name: {{ include "mina-vysper.fullname" . }}
  labels:
    {{- include "mina-vysper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mina-vysper.selectorLabels" . | nindent 4 }}
