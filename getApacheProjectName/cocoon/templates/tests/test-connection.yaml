apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cocoon.fullname" . }}-test-connection"
  labels:
    {{- include "cocoon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cocoon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
