apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xerces-for-cpp-xml-parser.fullname" . }}-test-connection"
  labels:
    {{- include "xerces-for-cpp-xml-parser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xerces-for-cpp-xml-parser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
