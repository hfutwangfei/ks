apiVersion: v1
kind: Service
metadata:
  name: {{ include "karaf.fullname" . }}
  labels:
    {{- include "karaf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "karaf.selectorLabels" . | nindent 4 }}
