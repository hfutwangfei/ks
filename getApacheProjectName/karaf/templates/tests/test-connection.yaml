apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "karaf.fullname" . }}-test-connection"
  labels:
    {{- include "karaf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "karaf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
