apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-stratos.fullname" . }}-test-connection"
  labels:
    {{- include "attic-stratos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-stratos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
