apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-stratos.fullname" . }}
  labels:
    {{- include "attic-stratos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-stratos.selectorLabels" . | nindent 4 }}
