apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-rdf.fullname" . }}-test-connection"
  labels:
    {{- include "commons-rdf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-rdf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
