apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-rdf.fullname" . }}
  labels:
    {{- include "commons-rdf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-rdf.selectorLabels" . | nindent 4 }}
