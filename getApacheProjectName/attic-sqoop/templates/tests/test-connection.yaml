apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-sqoop.fullname" . }}-test-connection"
  labels:
    {{- include "attic-sqoop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-sqoop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
