apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-forrest.fullname" . }}
  labels:
    {{- include "attic-forrest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-forrest.selectorLabels" . | nindent 4 }}
