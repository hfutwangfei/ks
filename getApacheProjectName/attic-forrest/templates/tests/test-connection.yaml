apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-forrest.fullname" . }}-test-connection"
  labels:
    {{- include "attic-forrest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-forrest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
