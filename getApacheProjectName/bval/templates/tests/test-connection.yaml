apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bval.fullname" . }}-test-connection"
  labels:
    {{- include "bval.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bval.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
