apiVersion: v1
kind: Service
metadata:
  name: {{ include "bval.fullname" . }}
  labels:
    {{- include "bval.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bval.selectorLabels" . | nindent 4 }}
