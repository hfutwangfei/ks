apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brooklyn.fullname" . }}-test-connection"
  labels:
    {{- include "brooklyn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brooklyn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
