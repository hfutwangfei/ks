apiVersion: v1
kind: Service
metadata:
  name: {{ include "brooklyn.fullname" . }}
  labels:
    {{- include "brooklyn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brooklyn.selectorLabels" . | nindent 4 }}
