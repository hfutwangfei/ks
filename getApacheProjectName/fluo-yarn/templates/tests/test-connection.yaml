apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluo-yarn.fullname" . }}-test-connection"
  labels:
    {{- include "fluo-yarn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluo-yarn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
