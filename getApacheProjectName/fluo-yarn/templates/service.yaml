apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluo-yarn.fullname" . }}
  labels:
    {{- include "fluo-yarn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluo-yarn.selectorLabels" . | nindent 4 }}
