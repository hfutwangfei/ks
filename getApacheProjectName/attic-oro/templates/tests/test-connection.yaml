apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-oro.fullname" . }}-test-connection"
  labels:
    {{- include "attic-oro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-oro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
