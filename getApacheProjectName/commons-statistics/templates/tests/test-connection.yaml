apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-statistics.fullname" . }}-test-connection"
  labels:
    {{- include "commons-statistics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-statistics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
