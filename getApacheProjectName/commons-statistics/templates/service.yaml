apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-statistics.fullname" . }}
  labels:
    {{- include "commons-statistics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-statistics.selectorLabels" . | nindent 4 }}
