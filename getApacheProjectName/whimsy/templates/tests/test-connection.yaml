apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whimsy.fullname" . }}-test-connection"
  labels:
    {{- include "whimsy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whimsy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
