apiVersion: v1
kind: Service
metadata:
  name: {{ include "whimsy.fullname" . }}
  labels:
    {{- include "whimsy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whimsy.selectorLabels" . | nindent 4 }}
