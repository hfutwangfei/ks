apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carbondata.fullname" . }}-test-connection"
  labels:
    {{- include "carbondata.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carbondata.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
