apiVersion: v1
kind: Service
metadata:
  name: {{ include "thrift.fullname" . }}
  labels:
    {{- include "thrift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thrift.selectorLabels" . | nindent 4 }}
