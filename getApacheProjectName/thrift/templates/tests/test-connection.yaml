apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thrift.fullname" . }}-test-connection"
  labels:
    {{- include "thrift.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thrift.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
