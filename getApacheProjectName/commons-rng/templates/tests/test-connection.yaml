apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-rng.fullname" . }}-test-connection"
  labels:
    {{- include "commons-rng.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-rng.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
