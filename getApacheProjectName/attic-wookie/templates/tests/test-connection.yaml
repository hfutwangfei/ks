apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-wookie.fullname" . }}-test-connection"
  labels:
    {{- include "attic-wookie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-wookie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
