apiVersion: v1
kind: Service
metadata:
  name: {{ include "httpcomponents-core.fullname" . }}
  labels:
    {{- include "httpcomponents-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "httpcomponents-core.selectorLabels" . | nindent 4 }}
