apiVersion: v1
kind: Service
metadata:
  name: {{ include "velocity-texen.fullname" . }}
  labels:
    {{- include "velocity-texen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "velocity-texen.selectorLabels" . | nindent 4 }}
