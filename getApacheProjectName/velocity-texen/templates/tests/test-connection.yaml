apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velocity-texen.fullname" . }}-test-connection"
  labels:
    {{- include "velocity-texen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "velocity-texen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
