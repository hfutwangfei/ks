apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kibble.fullname" . }}-test-connection"
  labels:
    {{- include "kibble.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kibble.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
