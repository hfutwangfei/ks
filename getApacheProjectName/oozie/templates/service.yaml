apiVersion: v1
kind: Service
metadata:
  name: {{ include "oozie.fullname" . }}
  labels:
    {{- include "oozie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oozie.selectorLabels" . | nindent 4 }}
