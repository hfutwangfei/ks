apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oozie.fullname" . }}-test-connection"
  labels:
    {{- include "oozie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oozie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
