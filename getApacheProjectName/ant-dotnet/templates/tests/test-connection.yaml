apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant-dotnet.fullname" . }}-test-connection"
  labels:
    {{- include "ant-dotnet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant-dotnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
