apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-dotnet.fullname" . }}
  labels:
    {{- include "ant-dotnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-dotnet.selectorLabels" . | nindent 4 }}
