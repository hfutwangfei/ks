apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-validator.fullname" . }}
  labels:
    {{- include "commons-validator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-validator.selectorLabels" . | nindent 4 }}
