apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-validator.fullname" . }}-test-connection"
  labels:
    {{- include "commons-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
