apiVersion: v1
kind: Service
metadata:
  name: {{ include "tapestry.fullname" . }}
  labels:
    {{- include "tapestry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tapestry.selectorLabels" . | nindent 4 }}
