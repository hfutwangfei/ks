apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tapestry.fullname" . }}-test-connection"
  labels:
    {{- include "tapestry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tapestry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
