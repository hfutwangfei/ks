apiVersion: v1
kind: Service
metadata:
  name: {{ include "xmlgraphics-batik.fullname" . }}
  labels:
    {{- include "xmlgraphics-batik.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xmlgraphics-batik.selectorLabels" . | nindent 4 }}
