apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xmlgraphics-batik.fullname" . }}-test-connection"
  labels:
    {{- include "xmlgraphics-batik.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xmlgraphics-batik.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
