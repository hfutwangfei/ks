apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-imaging.fullname" . }}-test-connection"
  labels:
    {{- include "commons-imaging.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-imaging.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
