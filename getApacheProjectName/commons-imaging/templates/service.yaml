apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-imaging.fullname" . }}
  labels:
    {{- include "commons-imaging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-imaging.selectorLabels" . | nindent 4 }}
