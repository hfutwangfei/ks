apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-jxpath.fullname" . }}
  labels:
    {{- include "commons-jxpath.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-jxpath.selectorLabels" . | nindent 4 }}
