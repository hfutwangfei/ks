apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-jxpath.fullname" . }}-test-connection"
  labels:
    {{- include "commons-jxpath.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-jxpath.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
