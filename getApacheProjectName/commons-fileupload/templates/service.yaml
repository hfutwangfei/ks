apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-fileupload.fullname" . }}
  labels:
    {{- include "commons-fileupload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-fileupload.selectorLabels" . | nindent 4 }}
