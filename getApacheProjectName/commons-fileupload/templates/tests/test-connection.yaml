apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-fileupload.fullname" . }}-test-connection"
  labels:
    {{- include "commons-fileupload.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-fileupload.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
