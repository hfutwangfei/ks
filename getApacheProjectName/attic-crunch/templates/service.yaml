apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-crunch.fullname" . }}
  labels:
    {{- include "attic-crunch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-crunch.selectorLabels" . | nindent 4 }}
