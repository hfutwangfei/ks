apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incubator-datafu.fullname" . }}-test-connection"
  labels:
    {{- include "incubator-datafu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incubator-datafu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
