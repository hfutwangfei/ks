apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-collections.fullname" . }}
  labels:
    {{- include "commons-collections.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-collections.selectorLabels" . | nindent 4 }}
