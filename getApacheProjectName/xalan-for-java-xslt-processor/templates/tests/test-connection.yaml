apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xalan-for-java-xslt-processor.fullname" . }}-test-connection"
  labels:
    {{- include "xalan-for-java-xslt-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xalan-for-java-xslt-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
