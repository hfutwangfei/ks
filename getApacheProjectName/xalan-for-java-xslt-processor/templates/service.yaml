apiVersion: v1
kind: Service
metadata:
  name: {{ include "xalan-for-java-xslt-processor.fullname" . }}
  labels:
    {{- include "xalan-for-java-xslt-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xalan-for-java-xslt-processor.selectorLabels" . | nindent 4 }}
