apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-esme.fullname" . }}
  labels:
    {{- include "attic-esme.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-esme.selectorLabels" . | nindent 4 }}
