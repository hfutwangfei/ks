apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-csv.fullname" . }}
  labels:
    {{- include "commons-csv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-csv.selectorLabels" . | nindent 4 }}
