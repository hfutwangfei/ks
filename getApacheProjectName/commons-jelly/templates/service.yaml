apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-jelly.fullname" . }}
  labels:
    {{- include "commons-jelly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-jelly.selectorLabels" . | nindent 4 }}
