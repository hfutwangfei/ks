apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "creadur-whisker.fullname" . }}-test-connection"
  labels:
    {{- include "creadur-whisker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "creadur-whisker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
