apiVersion: v1
kind: Service
metadata:
  name: {{ include "creadur-whisker.fullname" . }}
  labels:
    {{- include "creadur-whisker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "creadur-whisker.selectorLabels" . | nindent 4 }}
