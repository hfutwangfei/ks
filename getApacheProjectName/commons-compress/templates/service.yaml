apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-compress.fullname" . }}
  labels:
    {{- include "commons-compress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-compress.selectorLabels" . | nindent 4 }}
