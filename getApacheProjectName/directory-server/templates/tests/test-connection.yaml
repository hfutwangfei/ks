apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "directory-server.fullname" . }}-test-connection"
  labels:
    {{- include "directory-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "directory-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
