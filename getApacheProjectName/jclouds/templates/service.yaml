apiVersion: v1
kind: Service
metadata:
  name: {{ include "jclouds.fullname" . }}
  labels:
    {{- include "jclouds.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jclouds.selectorLabels" . | nindent 4 }}
