apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maven-doxia.fullname" . }}-test-connection"
  labels:
    {{- include "maven-doxia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maven-doxia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
