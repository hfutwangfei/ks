apiVersion: v1
kind: Service
metadata:
  name: {{ include "maven-doxia.fullname" . }}
  labels:
    {{- include "maven-doxia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maven-doxia.selectorLabels" . | nindent 4 }}
