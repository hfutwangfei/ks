apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "turbine.fullname" . }}-test-connection"
  labels:
    {{- include "turbine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "turbine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
