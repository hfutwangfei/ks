apiVersion: v1
kind: Service
metadata:
  name: {{ include "turbine.fullname" . }}
  labels:
    {{- include "turbine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "turbine.selectorLabels" . | nindent 4 }}
