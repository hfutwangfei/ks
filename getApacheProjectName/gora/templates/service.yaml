apiVersion: v1
kind: Service
metadata:
  name: {{ include "gora.fullname" . }}
  labels:
    {{- include "gora.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gora.selectorLabels" . | nindent 4 }}
