apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-jakarta-cactus.fullname" . }}-test-connection"
  labels:
    {{- include "attic-jakarta-cactus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-jakarta-cactus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
