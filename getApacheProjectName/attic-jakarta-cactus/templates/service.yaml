apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-jakarta-cactus.fullname" . }}
  labels:
    {{- include "attic-jakarta-cactus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-jakarta-cactus.selectorLabels" . | nindent 4 }}
