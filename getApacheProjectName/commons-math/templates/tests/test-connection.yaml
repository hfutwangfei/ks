apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-math.fullname" . }}-test-connection"
  labels:
    {{- include "commons-math.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-math.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
