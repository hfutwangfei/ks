apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-metamodel.fullname" . }}
  labels:
    {{- include "attic-metamodel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-metamodel.selectorLabels" . | nindent 4 }}
