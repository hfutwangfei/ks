apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-metamodel.fullname" . }}-test-connection"
  labels:
    {{- include "attic-metamodel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-metamodel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
