apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-beanutils.fullname" . }}
  labels:
    {{- include "commons-beanutils.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-beanutils.selectorLabels" . | nindent 4 }}
