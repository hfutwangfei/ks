apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-beanutils.fullname" . }}-test-connection"
  labels:
    {{- include "commons-beanutils.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-beanutils.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
