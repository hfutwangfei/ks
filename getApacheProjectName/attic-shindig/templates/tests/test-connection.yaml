apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-shindig.fullname" . }}-test-connection"
  labels:
    {{- include "attic-shindig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-shindig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
