apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-mrunit.fullname" . }}
  labels:
    {{- include "attic-mrunit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-mrunit.selectorLabels" . | nindent 4 }}
