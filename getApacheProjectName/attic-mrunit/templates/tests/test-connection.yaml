apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-mrunit.fullname" . }}-test-connection"
  labels:
    {{- include "attic-mrunit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-mrunit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
