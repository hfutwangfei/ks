apiVersion: v1
kind: Service
metadata:
  name: {{ include "attic-stanbol.fullname" . }}
  labels:
    {{- include "attic-stanbol.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attic-stanbol.selectorLabels" . | nindent 4 }}
