apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commons-ognl.fullname" . }}-test-connection"
  labels:
    {{- include "commons-ognl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commons-ognl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
