apiVersion: v1
kind: Service
metadata:
  name: {{ include "commons-ognl.fullname" . }}
  labels:
    {{- include "commons-ognl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commons-ognl.selectorLabels" . | nindent 4 }}
