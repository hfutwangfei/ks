apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portals.fullname" . }}-test-connection"
  labels:
    {{- include "portals.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portals.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
