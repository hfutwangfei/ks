apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ctakes.fullname" . }}-test-connection"
  labels:
    {{- include "ctakes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ctakes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
