apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spamassassin.fullname" . }}-test-connection"
  labels:
    {{- include "spamassassin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spamassassin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
