apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-predictionio.fullname" . }}-test-connection"
  labels:
    {{- include "attic-predictionio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-predictionio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
