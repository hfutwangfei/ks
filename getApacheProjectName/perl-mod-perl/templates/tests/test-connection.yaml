apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perl-mod-perl.fullname" . }}-test-connection"
  labels:
    {{- include "perl-mod-perl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perl-mod-perl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
