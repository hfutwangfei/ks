apiVersion: v1
kind: Service
metadata:
  name: {{ include "perl-mod-perl.fullname" . }}
  labels:
    {{- include "perl-mod-perl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perl-mod-perl.selectorLabels" . | nindent 4 }}
