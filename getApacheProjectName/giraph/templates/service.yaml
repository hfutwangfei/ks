apiVersion: v1
kind: Service
metadata:
  name: {{ include "giraph.fullname" . }}
  labels:
    {{- include "giraph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "giraph.selectorLabels" . | nindent 4 }}
