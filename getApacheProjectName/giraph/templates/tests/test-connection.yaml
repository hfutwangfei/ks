apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "giraph.fullname" . }}-test-connection"
  labels:
    {{- include "giraph.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "giraph.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
