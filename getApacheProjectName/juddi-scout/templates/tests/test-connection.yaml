apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "juddi-scout.fullname" . }}-test-connection"
  labels:
    {{- include "juddi-scout.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "juddi-scout.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
