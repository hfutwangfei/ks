apiVersion: v1
kind: Service
metadata:
  name: {{ include "juddi-scout.fullname" . }}
  labels:
    {{- include "juddi-scout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "juddi-scout.selectorLabels" . | nindent 4 }}
