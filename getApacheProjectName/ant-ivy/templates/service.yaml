apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-ivy.fullname" . }}
  labels:
    {{- include "ant-ivy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-ivy.selectorLabels" . | nindent 4 }}
