apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ant-compress.fullname" . }}-test-connection"
  labels:
    {{- include "ant-compress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ant-compress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
