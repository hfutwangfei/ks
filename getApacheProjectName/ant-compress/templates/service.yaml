apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-compress.fullname" . }}
  labels:
    {{- include "ant-compress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-compress.selectorLabels" . | nindent 4 }}
