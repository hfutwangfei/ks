apiVersion: v1
kind: Service
metadata:
  name: {{ include "geronimo.fullname" . }}
  labels:
    {{- include "geronimo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geronimo.selectorLabels" . | nindent 4 }}
