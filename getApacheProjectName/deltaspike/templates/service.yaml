apiVersion: v1
kind: Service
metadata:
  name: {{ include "deltaspike.fullname" . }}
  labels:
    {{- include "deltaspike.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deltaspike.selectorLabels" . | nindent 4 }}
