apiVersion: v1
kind: Service
metadata:
  name: {{ include "calcite.fullname" . }}
  labels:
    {{- include "calcite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calcite.selectorLabels" . | nindent 4 }}
