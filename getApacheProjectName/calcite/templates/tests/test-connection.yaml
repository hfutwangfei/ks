apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calcite.fullname" . }}-test-connection"
  labels:
    {{- include "calcite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calcite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
