apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lucene-pylucene.fullname" . }}-test-connection"
  labels:
    {{- include "lucene-pylucene.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lucene-pylucene.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
