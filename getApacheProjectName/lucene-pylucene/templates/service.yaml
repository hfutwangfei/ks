apiVersion: v1
kind: Service
metadata:
  name: {{ include "lucene-pylucene.fullname" . }}
  labels:
    {{- include "lucene-pylucene.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lucene-pylucene.selectorLabels" . | nindent 4 }}
