apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "attic-lucy.fullname" . }}-test-connection"
  labels:
    {{- include "attic-lucy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "attic-lucy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
