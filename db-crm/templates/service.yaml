apiVersion: v1
kind: Service
metadata:
  name: {{ include "db-crm.fullname" . }}
  labels:
    {{- include "db-crm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "db-crm.selectorLabels" . | nindent 4 }}
