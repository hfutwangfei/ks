apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-crm.fullname" . }}-test-connection"
  labels:
    {{- include "db-crm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-crm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
