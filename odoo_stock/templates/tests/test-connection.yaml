apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odoo_stock.fullname" . }}-test-connection"
  labels:
    {{- include "odoo_stock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odoo_stock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
