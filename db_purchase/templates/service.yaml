apiVersion: v1
kind: Service
metadata:
  name: {{ include "db_purchase.fullname" . }}
  labels:
    {{- include "db_purchase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "db_purchase.selectorLabels" . | nindent 4 }}
