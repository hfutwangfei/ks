apiVersion: v1
kind: Service
metadata:
  name: {{ include "odoo-change.fullname" . }}
  labels:
    {{- include "odoo-change.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odoo-change.selectorLabels" . | nindent 4 }}
