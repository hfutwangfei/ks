apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odoo-change.fullname" . }}-test-connection"
  labels:
    {{- include "odoo-change.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odoo-change.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
