apiVersion: v1
kind: Service
metadata:
  name: {{ include "db-stock.fullname" . }}
  labels:
    {{- include "db-stock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "db-stock.selectorLabels" . | nindent 4 }}
