apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-stock.fullname" . }}-test-connection"
  labels:
    {{- include "db-stock.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-stock.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
