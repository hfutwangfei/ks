apiVersion: v1
kind: Service
metadata:
  name: {{ include "odoo_purchase.fullname" . }}
  labels:
    {{- include "odoo_purchase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odoo_purchase.selectorLabels" . | nindent 4 }}
