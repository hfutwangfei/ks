apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dubhe.fullname" . }}-test-connection"
  labels:
    {{- include "dubhe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dubhe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
