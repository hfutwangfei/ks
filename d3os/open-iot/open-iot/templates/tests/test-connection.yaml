apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-iot.fullname" . }}-test-connection"
  labels:
    {{- include "open-iot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open-iot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
