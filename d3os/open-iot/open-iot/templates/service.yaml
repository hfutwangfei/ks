apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-iot.fullname" . }}
  labels:
    {{- include "open-iot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-iot.selectorLabels" . | nindent 4 }}
