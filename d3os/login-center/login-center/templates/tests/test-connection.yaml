apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "login-center.fullname" . }}-test-connection"
  labels:
    {{- include "login-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "login-center.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
