apiVersion: v1
kind: Service
metadata:
  name: {{ include "low-code.fullname" . }}
  labels:
    {{- include "low-code.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "low-code.selectorLabels" . | nindent 4 }}
