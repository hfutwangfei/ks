apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odoo-purchase.fullname" . }}-test-connection"
  labels:
    {{- include "odoo-purchase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odoo-purchase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
