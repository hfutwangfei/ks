apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db_crm.fullname" . }}-test-connection"
  labels:
    {{- include "db_crm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db_crm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
