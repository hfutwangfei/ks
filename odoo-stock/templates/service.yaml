apiVersion: v1
kind: Service
metadata:
  name: {{ include "odoo-stock.fullname" . }}
  labels:
    {{- include "odoo-stock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odoo-stock.selectorLabels" . | nindent 4 }}
