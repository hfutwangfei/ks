apiVersion: v1
kind: Service
metadata:
  name: {{ include "odoo_crm.fullname" . }}
  labels:
    {{- include "odoo_crm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odoo_crm.selectorLabels" . | nindent 4 }}
